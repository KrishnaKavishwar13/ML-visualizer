# ML-visualizer
Lightweight ML Model Visualizer is an interactive Streamlit app to train, test, and visualize ML models easily. Upload CSVs, preprocess data, select algorithms like SVM, Random Forest, or Logistic Regression, view metrics, test custom inputs, and download trained models—all in a sleek, gradient UI.
